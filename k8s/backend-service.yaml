apiVersion: v1
kind: Service
metadata:
  name: qa-metrics-backend
  namespace: qa-metrics
spec:
  type: ClusterIP
  selector:
    app: qa-metrics-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
